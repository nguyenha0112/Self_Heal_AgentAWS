apiVersion: v1
kind: Service
metadata:
  name: ai-engine
  namespace: self-heal-system
  labels:
    app: ai-engine
spec:
  type: ClusterIP
  selector:
    app: ai-engine
  ports:
    - name: http
      port: 8080
      targetPort: http
